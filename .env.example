# Starship Commander 环境变量配置 / Environment Variables
# Copy this file to .env and update the values / 复制此文件为 .env 并更新值

# ============ 局域网配置 / LAN Configuration ============
# 你的局域网 IP 地址 / Your LAN IP address
# 获取方式 / How to get:
#   macOS: ipconfig getifaddr en0
#   Linux: hostname -I | awk '{print $1}'
#   Windows: ipconfig | findstr IPv4
LAN_IP=192.168.1.100

# ============ 数据库配置 / Database Configuration ============
# 开发环境 / Development
DATABASE_URL="file:./dev.db"
# Docker 生产环境 / Docker Production
# DATABASE_URL="file:/app/prisma/dev.db"

# ============ 服务器配置 / Server Configuration ============
PORT=3001
NODE_ENV=development

# ============ 前端配置 / Frontend Configuration ============
# 前端端口 / Frontend port
VITE_PORT=3000

# 前端 URL（用于 CORS）/ Frontend URL (for CORS)
# 开发环境 / Development
FRONTEND_URL=http://localhost:3000
# 局域网部署 / LAN Deployment
# FRONTEND_URL=http://192.168.1.100:3000

# 前端访问后端的 URL / Frontend to Backend API URL
# 开发环境 / Development
VITE_API_URL=http://localhost:3001/trpc
# 局域网部署 / LAN Deployment
# VITE_API_URL=http://192.168.1.100:3001/trpc

# ============ JWT 配置 / JWT Configuration ============
# 生产环境请使用强随机字符串 / Use strong random string in production
# 生成方式: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# ============ 日志配置 / Logging Configuration ============
LOG_LEVEL=info
LOG_FORMAT=json

# ============ CORS 配置 / CORS Configuration ============
# 允许的前端地址 / Allowed frontend origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============ 可选配置 / Optional Configuration ============
# Redis Configuration (for caching, optional)
# REDIS_URL=redis://localhost:6379

# API Keys (for external services, optional)
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# GEMINI_API_KEY=your-gemini-api-key

