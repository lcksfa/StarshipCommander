# Starship Commander 生产环境配置 / Production Environment Configuration
# 部署前请根据实际情况修改以下配置 / Please modify these settings before deployment

# ============ 环境配置 / Environment Configuration ============
NODE_ENV=production

# ============ 服务器配置 / Server Configuration ============
# 前端端口（Vite Preview）/ Frontend port (Vite Preview)
VITE_PORT=3000

# 后端端口 / Backend port
BACKEND_PORT=3001

# 前端 URL（用于 CORS）/ Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# 前端访问后端的 URL / Frontend to Backend API URL
VITE_API_URL=http://localhost:3001/trpc

# ============ 数据库配置 / Database Configuration ============
# SQLite 数据库文件路径 / SQLite database file path
DATABASE_URL="file:./prisma/dev.db"

# ============ JWT 配置 / JWT Configuration ============
# JWT 密钥（⚠️ 生产环境必须修改为随机字符串）/ JWT secret (⚠️ MUST CHANGE in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
# JWT 过期时间 / JWT expiration time
JWT_EXPIRES_IN=7d

# ============ CORS 配置 / CORS Configuration ============
# 允许的域名（逗号分隔）/ Allowed origins (comma-separated)
# 如果有域名，替换为实际域名 / If you have a domain, replace with actual domain
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com

# ============ 日志配置 / Logging Configuration ============
# 日志级别 / Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============ 应用配置 / Application Configuration ============
# 应用名称 / App name
APP_NAME="Starship Commander"

# 应用 URL / App URL
# 如果有域名，替换为实际域名 / If you have a domain, replace with actual domain
# APP_URL=https://your-domain.com
APP_URL=http://localhost:3000

# ============ 安全配置 / Security Configuration ============
# 密码加密盐轮数 / Password salt rounds
BCRYPT_ROUNDS=10
