# pnpm 快速使用指南

## 🚀 项目已成功配置为使用 pnpm

### 基本命令

```bash
# 安装依赖
pnpm install

# 启动前端开发服务器
pnpm dev

# 启动后端开发服务器
pnpm dev:backend

# 同时启动前后端
pnpm dev:all

# 构建项目
pnpm build:all

# 生产环境启动
pnpm start:prod
```

### 📦 包管理

```bash
# 安装新包
pnpm add <package-name>

# 安装开发依赖
pnpm add <package-name> -D

# 移除包
pnpm remove <package-name>

# 更新包
pnpm update

# 检查过期包
pnpm outdated

# 安全审计
pnpm audit
```

### 🧹 清理命令

```bash
# 清理构建文件
pnpm clean

# 完全重新安装
pnpm reinstall

# 清理缓存
pnpm store prune
```

### 🎯 常用脚本

| 脚本 | 用途 |
|------|------|
| `pnpm dev` | 启动前端开发服务器 |
| `pnpm dev:backend` | 启动后端开发服务器 |
| `pnpm dev:all` | 同时启动前后端 |
| `pnpm build` | 构建前端 |
| `pnpm build:backend` | 构建后端 |
| `pnpm test` | 运行测试 |
| `pnpm lint` | 代码检查和修复 |
| `pnpm format` | 代码格式化 |

## ⚡ 性能优势

- **速度**: 比 npm 快 2-3 倍
- **存储**: 共享依赖，节省 50%+ 磁盘空间
- **安全**: 内置安全审计和严格依赖解析
- **兼容**: 完全兼容 npm 生态系统

## 🔧 配置文件

- `.pnpm-workspace.yaml` - 工作空间配置
- `.npmrc` - pnpm 全局配置
- `.pnpmfile.js` - 自定义安装钩子

## ✅ 验证安装

项目已成功迁移到 pnpm，所有功能正常：

- ✅ 依赖安装成功 (898 packages in 1m 6.5s)
- ✅ 后端编译成功
- ✅ 后端服务启动正常 (端口 3003)
- ✅ tRPC API 正常工作
- ✅ 所有脚本正常执行

现在你可以使用 `pnpm` 替代 `npm` 进行所有包管理操作！