# Starship Commander 生产环境配置
# Production environment configuration

# ============ 环境配置 / Environment ============
NODE_ENV=production

# ============ 服务器配置 / Server Configuration ============
# 前端端口 / Frontend port
VITE_PORT=3000
# 后端端口 / Backend port
BACKEND_PORT=3001
# 后端 URL / Backend URL
VITE_API_URL=http://localhost:3001

# ============ 数据库配置 / Database Configuration ============
# SQLite 数据库文件路径 / SQLite database file path
DATABASE_URL="file:./prisma/dev.db"

# ============ JWT 配置 / JWT Configuration ============
# JWT 密钥（必须修改为随机字符串）/ JWT secret (must be changed)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# JWT 过期时间 / JWT expiration time
JWT_EXPIRES_IN=7d

# ============ CORS 配置 / CORS Configuration ============
# 允许的域名 / Allowed origins
CORS_ORIGINS=http://localhost:3000,http://your-domain.com

# ============ 日志配置 / Logging Configuration ============
# 日志级别 / Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============ 应用配置 / Application Configuration ============
# 应用名称 / App name
APP_NAME="Starship Commander"
# 应用 URL / App URL
APP_URL=http://your-domain.com

# ============ 安全配置 / Security Configuration ============
# 会话密钥 / Session secret
SESSION_SECRET=your-super-secret-session-key-change-this
# 密码加密盐轮数 / Password salt rounds
BCRYPT_ROUNDS=10

# ============ 邮件配置（可选）/ Email Configuration (Optional) ============
# SMTP 服务器 / SMTP server
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@your-domain.com
